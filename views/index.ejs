<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hub Cron - Pull those open source images from DockerHub so they dont get deleted</title>
    
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css"
    />
    <link href="/style.css" rel="stylesheet"/>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <p>Beep Beep! I am the DockerHub cron bot └[∵┌]└[ ∵ ]┘[┐∵]┘</p>
        <p>I pull your public free docker images once every 6 months - 1 day.</p>
        <form action="/addImage" method="POST">
          <div class="col-md-offset-2 col-md-6 col-sm-offset-1 col-sm-7">
            <label for="dimage">youruser/yourimage:</label><br />
            <input type="text"  class="form-control" id="dimage" name="dimage" /><br />
            <input type="submit" value="Gimme Images" />
          </div>
        </form>
      </div>

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <script>
      function moveTask (event, id){
        const{name} = event.target;

        fetch("http://localhost:8080/moveTaskDone", {
          method: "PUT",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({
            name,
            id
          })
        }).then(response => response.json()).then(status=> {
          console.log(status);
          window.location.reload()

        }).catch(err => console.log(err))
      }
    </script>
  </body>
</html>
